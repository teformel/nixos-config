{
    "layer": "top",
    "position": "top",
    "margin-top": 5,
    "margin-left": 10,
    "margin-right": 10,
    "height": 30,
    "spacing": 4,

    // === 📦 布局变化 ===
    // 左边加了 launcher，右边加了 power
    "modules-left": ["custom/launcher", "hyprland/workspaces", "hyprland/window"],
    "modules-center": ["clock"],
    "modules-right": ["backlight" ,"pulseaudio", "pulseaudio#microphone", "group/hardware", "battery", "tray", "custom/power"],
    
    // === 🚀 新增：开始菜单按钮 ===
    "custom/launcher": {
        "format": " ", // NixOS 的图标
        "on-click": "wofi --show drun", // 点击打开 Wofi
        "tooltip": false
    },

    // === 🔴 新增：电源菜单按钮 ===
    "custom/power": {
        "format": "⏻ ",
        "on-click": "wlogout", // 点击打开 wlogout
        "tooltip": false
    },

    // === 📦 新增：硬件监控抽屉 ===
    "group/hardware": {
        "orientation": "horizontal",
        "modules": ["cpu", "memory"], // 抽屉里放什么
        "drawer": {
            "transition-duration": 500, // 动画速度
            "children-class": "not-power",
            "transition-left-to-right": false
        }
    },

    "hyprland/workspaces": {
        "disable-scroll": true,
        "all-outputs": true,
        "format": "{icon}",
        "format-icons": {
            "default": "○",
            "active": "●"
        }
    },

    "hyprland/window": {
        "max-length": 40,
        "separate-outputs": true
    },

    // === 🖱️ 升级：时间 (支持点击) ===
    "clock": {
        "format": " {:%H:%M  📅 %m-%d}",
        "tooltip-format": "<big>{:%Y %B}</big>\n<tt><small>{calendar}</small></tt>"
        // 如果你没装 gnome-calendar，这行暂时没用，或者换成简单的 cal 命令
        // "on-click": "gnome-calendar" 
    },

    // === 🖱️ 升级：CPU (点击打开任务管理器) ===
    "cpu": {
        "format": " {usage}%",
        "tooltip": false,
        "on-click": "kitty btop" // 点击打开 btop
    },

    // === 🖱️ 升级：内存 (点击打开任务管理器) ===
    "memory": {
        "format": " {}%",
        "on-click": "kitty btop"
    },

    "battery": {
        "states": {
            "warning": 30,
            "critical": 15
        },
        "format": "{icon} {capacity}%",
        "format-charging": "⚡ {capacity}%",
        "format-plugged": " {capacity}%",
        "format-icons": ["", "", "", "", ""]
    },

    "pulseaudio": {
        // 👇 去掉了 {volume}%，只留图标
        "format": "{icon}", 
        "format-muted": "🔇",
        "format-icons": {
            "default": ["", "", ""]
        },
        "tooltip-format": "Volume: {volume}%", // 鼠标放上去看数字
        "on-click": "pavucontrol"
    },
    
    "tray": {
        "icon-size": 18,
        "spacing": 10
    },

    // === 💡 新增：亮度调节 ===
    "backlight": {
        "device": "intel_backlight", // 大部分笔记本是这个，如果不行删掉这行自动识别
        "format": "{icon} {percent}%",
        "format-icons": ["🌑", "🌘", "🌗", "🌖", "🌕"],
        // ✨ 启用滚轮调节 (向上滚加亮度，向下滚减亮度)
        "on-scroll-up": "brightnessctl set +5%",
        "on-scroll-down": "brightnessctl set 5%-",
        "on-click": "brightnessctl set 100%", // 点一下变最亮
        "tooltip": false
    },
    // === 🎤 新增：麦克风监控 ===
    "pulseaudio#microphone": {
        "format": "{format_source}",
        // 👇 去掉了 {volume}%，只留图标
        "format-source": "", 
        "format-source-muted": "",
        "on-click": "wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle",
        "on-scroll-up": "wpctl set-volume @DEFAULT_AUDIO_SOURCE@ 5%+",
        "on-scroll-down": "wpctl set-volume @DEFAULT_AUDIO_SOURCE@ 5%-",
        "tooltip-format": "Mic: {source_volume}%"
    }
}